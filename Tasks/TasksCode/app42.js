/*
Нам нужна функция, которая может преобразовать число (целое число) в строку.

Какие способы достижения этого вы знаете?

Примеры (ввод --> вывод):
*/

function numberToString(num) {
   return num.toString();
}

let n = numberToString(123);
console.log(n);